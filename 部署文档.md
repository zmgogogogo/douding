# 豆丁 (Douding) 部署文档

> 服务器：阿里云 Alibaba Cloud Linux 3  
> 地址：47.109.29.158 | 域名：douding.online  
> SSH：`admin@47.109.29.158`

---

## 零、架构概览

```
浏览器 → Nginx (:443) → Java Spring Boot (:8080) → Python FastAPI (:3457)
                           ↑                          ↑
                      主业务逻辑                  图片转像素图引擎
                      （MySQL+Redis）            （OpenCV+K-Means）
```

| 服务 | 端口 | 技术栈 | 用途 |
|------|------|--------|------|
| Nginx | 80/443 | Nginx | 反向代理 + 静态文件 |
| Java 后端 | 8080 | Spring Boot 3 + Maven | 主业务 API |
| Python 后端 | 3457 | FastAPI + OpenCV | 图片转像素图引擎 |
| MySQL | 3306 | MySQL 8.0 | 数据库 |
| Redis | 6379 | Redis | 缓存 / Session |

> 图片转像素图请求链路：`Nginx → Java(8080) → PythonProxyController → Python(3457)`

---

## 一、服务器环境

### 1.1 安装依赖

```bash
# Java 17 JDK
sudo dnf install -y java-17-openjdk-devel

# Python 3.11（图片转像素图引擎）
sudo dnf install -y python3.11 python3.11-pip python3.11-devel

# MySQL 8.0
sudo dnf install -y mysql-server
sudo systemctl start mysqld && sudo systemctl enable mysqld

# Redis
sudo dnf install -y redis
sudo systemctl start redis && sudo systemctl enable redis

# Nginx（通常已预装）
sudo dnf install -y nginx
sudo systemctl start nginx && sudo systemctl enable nginx

# Maven 3.6（系统自带版本足够，pom.xml 已做兼容处理）
sudo dnf install -y maven
```

### 1.1.1 安装 Python 依赖

```bash
# 创建虚拟环境（可选，推荐）
python3.11 -m venv /home/admin/server-python/venv
source /home/admin/server-python/venv/bin/activate

# 安装依赖
pip install -r /home/admin/server-python/requirements.txt
```

> ⚠️ **Python 版本要求**：图片转像素图引擎要求 Python 3.10+，Alibaba Cloud Linux 3 默认 Python 3.6 不满足。<br>
> 如果 `dnf` 中没有 `python3.11`，请先安装 EPEL：
> ```bash
> sudo dnf install -y epel-release
> ```

### 1.2 切换 Java 17 为默认

```bash
sudo alternatives --set java java-17-openjdk.x86_64
sudo alternatives --set javac java-17-openjdk.x86_64
```

**JAVA_HOME 路径：** `/usr/lib/jvm/java-17-openjdk-17.0.19.0.10-1.0.2.1.al8.x86_64`

> 注意：若版本号有变化，用 `ls /usr/lib/jvm/` 查看实际路径。

### 1.3 关键路径

| 用途 | 路径 |
|------|------|
| Java 源码 | `/home/admin/server-java/` |
| JAR 文件 | `/home/admin/server-java/target/douding-server-1.0.0-SNAPSHOT.jar` |
| Python 源码 | `/home/admin/server-python/` |
| Python 虚拟环境 | `/home/admin/server-python/venv/` |
| 前端静态文件 | `/var/www/douding/` |
| Java 应用日志 | `/home/admin/server-java/logs/` |
| Python 应用日志 | `/home/admin/server-python/logs/` |
| Nginx 配置 | `/etc/nginx/conf.d/douding.conf` |
| Java Systemd 服务 | `/etc/systemd/system/douding.service` |
| Python Systemd 服务 | `/etc/systemd/system/douding-python.service` |

---

## 二、数据库

### 2.1 创建数据库

```bash
mysql -u root -e 'CREATE DATABASE IF NOT EXISTS douding CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;'
```

### 2.2 清空重建（仅开发/初始化）

```bash
mysql -u root -e 'DROP DATABASE IF EXISTS douding; CREATE DATABASE douding CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;'
```

### 2.3 MySQL 连接配置

生产环境配置在 `application-prod.yml` 中，通过 systemd 环境变量注入：

```
DB_URL=jdbc:mysql://localhost:3306/douding?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
DB_USERNAME=root
DB_PASSWORD=
```

---

## 三、迁移脚本兼容性（重要！）

> **MySQL 严格模式不允许 TEXT / JSON 列有默认值。** 编写新迁移时注意：

```sql
-- ❌ 错误（MySQL 不兼容）
description TEXT DEFAULT '',
permissions JSON DEFAULT '[]',

-- ✅ 正确
description TEXT,
permissions JSON,
```

现有 `pom.xml` 已做 Maven 3.6.2 兼容性覆盖：

```xml
<properties>
    <!-- 兼容服务器 Maven 3.6.2（Spring Boot 3.3.2 默认要求 3.6.3+） -->
    <maven-compiler-plugin.version>3.10.1</maven-compiler-plugin.version>
    <maven-jar-plugin.version>3.3.0</maven-jar-plugin.version>
    <maven-resources-plugin.version>3.3.1</maven-resources-plugin.version>
    <maven-surefire-plugin.version>3.1.2</maven-surefire-plugin.version>
</properties>
```

生产配置覆盖 Flyway 使用 MySQL 迁移脚本（而非 H2）：

```yaml
spring:
  flyway:
    locations: classpath:db/migration    # MySQL 版本
    # 开发环境用 classpath:db/migration-h2
```

---

## 四、部署流程

### 4.1 完整部署（本地 → 服务器）

```bash
# 0. 本地提交代码
git add -A && git commit -m "release" && git push

# 1. 上传 Java 源码
rsync -avz --exclude 'data/' --exclude 'target/' \
  ./server-java/ admin@47.109.29.158:/home/admin/server-java/

# 2. 上传 Python 源码
rsync -avz --exclude '__pycache__/' --exclude 'venv/' \
  ./server-python/ admin@47.109.29.158:/home/admin/server-python/

# 3. 构建前端（如前端有改动）
npm run build

# 4. 上传前端
rsync -avz ./dist/ admin@47.109.29.158:/var/www/douding/

# 5. 构建 JAR + 安装 Python 依赖 + 重启所有服务
ssh admin@47.109.29.158 "
  # Java 构建
  JAVA_HOME=/usr/lib/jvm/java-17-openjdk-17.0.19.0.10-1.0.2.1.al8.x86_64 \
    /usr/bin/mvn -f /home/admin/server-java/pom.xml package -DskipTests -q

  # Python 依赖（如有新增）
  /home/admin/server-python/venv/bin/pip install -r /home/admin/server-python/requirements.txt -q

  # 清库重建（如迁移脚本有改动）
  mysql -u root -e 'DROP DATABASE IF EXISTS douding; CREATE DATABASE douding CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;'

  # 重启服务
  sudo systemctl restart douding
  sudo systemctl restart douding-python
"
```

### 4.2 快速重新部署（仅代码更新）

```bash
# 上传 Java 源码 → 构建 JAR → 重启
rsync -avz --exclude 'data/' --exclude 'target/' ./server-java/ admin@47.109.29.158:/home/admin/server-java/
ssh admin@47.109.29.158 "
  sudo systemctl stop douding
  rm -f /home/admin/server-java/target/douding-server-1.0.0-SNAPSHOT.jar
  JAVA_HOME=/usr/lib/jvm/java-17-openjdk-17.0.19.0.10-1.0.2.1.al8.x86_64 \
    /usr/bin/mvn -f /home/admin/server-java/pom.xml package -DskipTests -q
  sudo systemctl start douding
"

# 上传 Python 源码 → 重启（仅当 Python 有改动）
rsync -avz --exclude '__pycache__/' --exclude 'venv/' \
  ./server-python/ admin@47.109.29.158:/home/admin/server-python/
ssh admin@47.109.29.158 "
  /home/admin/server-python/venv/bin/pip install -r /home/admin/server-python/requirements.txt -q
  sudo systemctl restart douding-python
"
```

> ⚠️ **注意**：如果迁移脚本有改动，需要在步骤中加入 `DROP DATABASE` 清库重建。

### 4.3 仅更新前端

```bash
npm run build
rsync -avz ./dist/ admin@47.109.29.158:/var/www/douding/
```

前端更新无需重启后端，Nginx 直接 serve 静态文件。

### 4.4 仅更新配置

```bash
scp deploy/douding.service admin@47.109.29.158:/tmp/
ssh admin@47.109.29.158 "sudo mv /tmp/douding.service /etc/systemd/system/ && sudo systemctl daemon-reload && sudo systemctl restart douding"
```

---

## 五、服务管理

### 5.1 常用命令

```bash
# === Java 后端 ===
sudo systemctl status douding
sudo systemctl start douding
sudo systemctl stop douding
sudo systemctl restart douding

# === Python 图片引擎 ===
sudo systemctl status douding-python
sudo systemctl start douding-python
sudo systemctl stop douding-python
sudo systemctl restart douding-python

# === 查看日志 ===
sudo journalctl -u douding -f                    # Java 实时系统日志
sudo journalctl -u douding-python -f             # Python 实时系统日志
tail -f /home/admin/server-java/logs/stdout.log  # Java 应用日志
tail -f /home/admin/server-python/logs/stdout.log  # Python 应用日志

# === 检查端口 ===
ss -tlnp | grep -E '8080|3457'

# === 测试 API ===
# Java 后端
curl -s http://localhost:8080/api/beads/colors
# Python 引擎健康检查
curl -s http://localhost:3457/api/health
# 公网测试
curl -s https://douding.online/api/beads/colors
```

### 5.2 重载 Nginx

```bash
sudo nginx -t                          # 检查配置
sudo systemctl reload nginx            # 热重载
```

### 5.3 首次安装 systemd 服务

```bash
scp deploy/douding.service admin@47.109.29.158:/tmp/
ssh admin@47.109.29.158 "
  sudo mv /tmp/douding.service /etc/systemd/system/
  sudo systemctl daemon-reload
  sudo systemctl enable douding
  sudo systemctl start douding
"
```

---

## 六、Nginx 配置

配置文件：`/etc/nginx/conf.d/douding.conf`

```nginx
upstream douding_backend {
    server 127.0.0.1:8080;
    keepalive 32;
}

server {
    server_name douding.online www.douding.online 47.109.29.158;
    client_max_body_size 50M;
    root /var/www/douding;
    index index.html;

    # API 代理到 Spring Boot
    location /api/ {
        proxy_pass http://douding_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
    }

    # WebSocket 协作
    location /ws/ {
        proxy_pass http://douding_backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # 上传文件
    location /uploads/ {
        alias /var/www/douding/uploads/;
    }

    # SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }

    listen 443 ssl;
    ssl_certificate /etc/letsencrypt/live/douding.online/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/douding.online/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
}

server {
    if ($host = www.douding.online) { return 301 https://$host$request_uri; }
    if ($host = douding.online) { return 301 https://$host$request_uri; }
    listen 80;
    server_name douding.online www.douding.online 47.109.29.158;
    return 404;
}
```

更新配置：

```bash
scp deploy/douding-nginx.conf admin@47.109.29.158:/tmp/
ssh admin@47.109.29.158 "sudo cp /tmp/douding-nginx.conf /etc/nginx/conf.d/douding.conf && sudo nginx -t && sudo systemctl reload nginx"
```

---

## 七、Systemd 服务配置

### 7.1 Java 后端服务

文件：`deploy/douding.service`（项目仓库中）

```ini
[Unit]
Description=豆丁 Douding Spring Boot 后端服务
After=network.target mysql.service redis.service
Wants=mysql.service redis.service

[Service]
Type=simple
User=admin
WorkingDirectory=/home/admin/server-java

Environment=JAVA_HOME=/usr/lib/jvm/java-17-openjdk-17.0.19.0.10-1.0.2.1.al8.x86_64
Environment=SPRING_PROFILES_ACTIVE=prod
Environment=DB_URL=jdbc:mysql://localhost:3306/douding?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
Environment=DB_USERNAME=root
Environment=DB_PASSWORD=
Environment=REDIS_HOST=localhost
Environment=REDIS_PORT=6379
Environment=REDIS_PASSWORD=
Environment=JWT_SECRET=douding-jwt-secret-change-me-in-production
Environment=JWT_ADMIN_SECRET=douding-admin-jwt-secret-change-me
Environment=PORT=8080

ExecStart=/usr/lib/jvm/java-17-openjdk-17.0.19.0.10-1.0.2.1.al8.x86_64/bin/java -jar /home/admin/server-java/target/douding-server-1.0.0-SNAPSHOT.jar
ExecStop=/bin/kill -15 $MAINPID

Restart=always
RestartSec=10

StandardOutput=append:/home/admin/server-java/logs/stdout.log
StandardError=append:/home/admin/server-java/logs/stderr.log

[Install]
WantedBy=multi-user.target
```

### 7.2 Python 图片引擎服务

文件：`deploy/douding-python.service`（需创建）

```ini
[Unit]
Description=豆丁 Douding Python 图片转像素图引擎
After=network.target douding.service
Wants=douding.service

[Service]
Type=simple
User=admin
WorkingDirectory=/home/admin/server-python

Environment=PORT=3457
Environment=NODE_ENV=production
Environment=JWT_SECRET=douding-jwt-secret-change-me-in-production

ExecStart=/home/admin/server-python/venv/bin/python main.py
ExecStop=/bin/kill -15 $MAINPID

Restart=always
RestartSec=5

StandardOutput=append:/home/admin/server-python/logs/stdout.log
StandardError=append:/home/admin/server-python/logs/stderr.log

[Install]
WantedBy=multi-user.target
```

首次安装：

```bash
# 上传服务文件
scp deploy/douding-python.service admin@47.109.29.158:/tmp/

# 安装并启动
ssh admin@47.109.29.158 "
  mkdir -p /home/admin/server-python/logs
  sudo mv /tmp/douding-python.service /etc/systemd/system/
  sudo systemctl daemon-reload
  sudo systemctl enable douding-python
  sudo systemctl start douding-python
"
```

---

## 八、故障排查

### 常见问题

| 现象 | 原因 | 解决 |
|------|------|------|
| Maven 报不支持发行版本 17 | 系统默认 Java 是 11 | 设置 `JAVA_HOME` 指向 Java 17 |
| `TEXT can't have a default value` | MySQL 严格模式 | 移除 TEXT/JSON 列的 DEFAULT |
| Flyway `Validate failed` | 迁移校验失败 | 清空数据库重建让 Flyway 重跑 |
| 端口 8080 不监听 | Java 应用启动失败 | `journalctl -u douding -n 50` 看日志 |
| 端口 3457 不监听 | Python 引擎未启动 | `journalctl -u douding-python -n 50` 看日志 |
| JAR 只读无法覆盖 | 服务正在运行 | 先 `systemctl stop douding` 再构建 |
| 图片转换返回 502 | Python 引擎挂了 | `sudo systemctl restart douding-python` |
| 图片转换超时 | 图片过大或颜色数过多 | 降低图片尺寸或颜色数（建议 ≤16） |
| 图片转换返回 422 | Spring 提前消费了 multipart 流 | 确保 `resolve-lazily: true` |
| Python 依赖缺失 | 新增了 pip 包 | 在服务器上重新 `pip install` |
| pip 找不到 | 未激活虚拟环境 | 使用完整路径 `/home/admin/server-python/venv/bin/pip` |

### 日志排查

```bash
# Java 启动错误
tail -100 /home/admin/server-java/logs/stdout.log | grep -E "Caused|Error|Exception"

# Python 引擎错误
tail -100 /home/admin/server-python/logs/stdout.log | grep -E "Error|Traceback|Exception"

# 系统服务日志
sudo journalctl -u douding --no-pager -n 50
sudo journalctl -u douding-python --no-pager -n 50

# 排查迁移问题
mysql -u root douding -e "SELECT * FROM flyway_schema_history;"

# 测试图片转换链路
curl -s http://localhost:3457/api/health
```

---

## 九、环境变量速查

### Java 后端

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `PORT` | `8080` | Java 应用端口 |
| `SPRING_PROFILES_ACTIVE` | `prod` | 激活的配置 profile |
| `DB_URL` | `jdbc:mysql://localhost:3306/douding?...` | 数据库连接 |
| `DB_USERNAME` | `root` | 数据库用户 |
| `DB_PASSWORD` | 空 | 数据库密码 |
| `REDIS_HOST` | `localhost` | Redis 地址 |
| `REDIS_PORT` | `6379` | Redis 端口 |
| `REDIS_PASSWORD` | 空 | Redis 密码 |
| `JWT_SECRET` | — | JWT 签名密钥 |
| `JWT_ADMIN_SECRET` | — | 管理后台 JWT 密钥 |

### Python 图片引擎

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `PORT` | `3457` | Python 引擎端口 |
| `NODE_ENV` | `production` | 运行环境 |
| `JWT_SECRET` | — | 与 Java 共用同一密钥 |

---

## 十、一键部署脚本

仓库 `deploy/` 目录下保存了所有部署相关文件：

```
deploy/
├── douding-nginx.conf       # Nginx 配置
├── douding.service          # Java systemd 服务
├── douding-python.service   # Python systemd 服务
├── clean.sql                # 数据库清空脚本
├── deploy-v3.sh             # Java 部署脚本（清库+重启+验证）
├── server-setup.sh          # 服务器初始化脚本
└── sync-from-local.sh       # 本地全量同步脚本
```

下次部署只需：

```bash
# === 完整部署（本地 → 服务器） ===
# 1. 上传代码
rsync -avz --exclude 'data/' --exclude 'target/' ./server-java/ admin@47.109.29.158:/home/admin/server-java/
rsync -avz --exclude '__pycache__/' --exclude 'venv/' ./server-python/ admin@47.109.29.158:/home/admin/server-python/
npm run build
rsync -avz ./dist/ admin@47.109.29.158:/var/www/douding/

# 2. 部署并重启
ssh admin@47.109.29.158 "
  # Java 构建
  JAVA_HOME=/usr/lib/jvm/java-17-openjdk-17.0.19.0.10-1.0.2.1.al8.x86_64 \
    /usr/bin/mvn -f /home/admin/server-java/pom.xml package -DskipTests -q
  # Python 依赖
  /home/admin/server-python/venv/bin/pip install -r /home/admin/server-python/requirements.txt -q
  # 重启
  sudo systemctl restart douding douding-python
"
```
