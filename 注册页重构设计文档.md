# 豆丁注册页重构设计文档

## 一、变更概述

将现有的"用户名/手机号双模式注册"简化为"手机号唯一注册方式"，增加安全性和用户体验。

### 变更原因
- 手机号注册可防止机器人批量注册
- 短信验证 + 双密码确认降低误操作
- 协议勾选满足合规要求
- 简化注册流程，降低用户决策成本

---

## 二、页面设计

### 布局：居中卡片

```
┌─────────────────────────────────┐
│          🧩 豆丁                 │  Logo
│       创建你的拼豆账号            │
│                                 │
│  [手机号输入框]                   │  11位
│  [验证码输入] [获取验证码]        │  6位 + 60s倒计时
│  [设置密码]                 👁  │  ≥6位
│  [确认密码]                 👁  │  实时校验一致性
│  [昵称（可选）]                  │
│                                 │
│  ☐ 已阅读并同意《用户协议》《隐私政策》│
│                                 │
│  [═══════ 注 册 ═══════]        │  主按钮，条件激活
│                                 │
│     已有账号？去登录              │
└─────────────────────────────────┘
```

### 配色
- 主色 #0058BC（品牌蓝）
- 背景渐变 from-blue-50 via-slate-50 to-blue-50/40
- 卡片白色 16px 圆角，细阴影

---

## 三、交互规则

### 3.1 按钮激活条件

注册按钮在以下条件**全部满足**时才可点击：
1. 手机号格式正确（11位，1开头）
2. 验证码已发送且已填写6位
3. 密码 ≥ 6位
4. 确认密码与密码一致
5. 协议已勾选

### 3.2 实时校验

| 字段 | 校验 | 反馈 |
|------|------|------|
| 手机号 | 正则 /^1[3-9]\d{9}$/ | 格式错→红框 |
| 验证码 | 6位数字 | 无特殊校验 |
| 密码 | ≥6位 | 少于6位→红框+提示 |
| 确认密码 | === 密码 | 不一致→红框+"两次密码不一致"；一致→绿框 ✓ |
| 协议 | 必须勾选 | 未勾选→按钮灰色 |

### 3.3 后端校验

| 场景 | HTTP | 提示 |
|------|------|------|
| 验证码错误/过期 | 400 | "验证码错误或已过期" |
| 手机号已注册 | 409 | "该手机号已注册，请直接登录" |
| 密码少于6位 | 400 | "密码至少需要6位" |
| 两次密码不一致 | 400 | "两次密码不一致" |

---

## 四、API 接口

### 注册（无变化）

```
POST /api/auth/register
{
  "phone": "13812345678",
  "code": "123456",
  "password": "abc123",
  "confirmPassword": "abc123",
  "nickname": "豆友张三"
}
→ 200 { token, user }
→ 400 验证码错误 / 密码不一致
→ 409 手机号已注册
```

### 发送验证码（无变化）

```
POST /api/auth/send-code
{ "phone": "13812345678" }
→ 200
→ 400 手机号格式错 / 频率限制
```

---

## 五、协议弹窗

点击《用户协议》或《隐私政策》→ 弹出 Dialog 展示协议全文（内置文本，无需加载外部页面）。

协议内容需律师审核，当前使用简版占位：

**用户协议**
1. 用户注册即表示同意本协议
2. 用户需对上传内容负责，不得上传违法信息
3. 平台有权对违规内容进行下架处理
4. 用户发布的作品默认允许其他用户浏览

**隐私政策**
1. 手机号仅用于账号安全和找回密码，不会公开
2. 用户数据存储于阿里云服务器
3. 用户可随时申请注销账号并删除数据

---

## 六、文件变更清单

| 文件 | 变更 |
|------|------|
| `server-java/.../dto/RegisterDTO.java` | 加 confirmPassword，username 改为可选 |
| `server-java/.../service/AuthService.java` | 加密码一致性校验，手机号注册为主流程 |
| `src/api/index.js` | 无需变化（API 路径不变） |
| `src/views/AuthView.vue` | 重写注册部分 |

---

## 七、登录页保持不变

登录页保留现有的双模式：
- 用户名 + 密码
- 手机号 + 验证码
