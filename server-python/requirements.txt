# ============================================
# 豆丁智能转图 — Python 后端依赖
# Python 3.9+ (推荐 3.12) | FastAPI | OpenCV
# ============================================

# --- Web 框架 ---
fastapi>=0.100.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
python-jose[cryptography]>=3.3.0

# --- 图像处理 ---
opencv-python-headless>=4.8.0
numpy>=1.24.0,<2.0
scikit-learn>=1.3.0
scipy>=1.10.0
Pillow>=10.0.0

# --- 语义分割（可选：需要下载模型） ---
# paddleseg  # 需要 PaddlePaddle 环境
onnxruntime>=1.17.0

# --- 人脸检测（可选：需要下载模型） ---
# retina-face  # 需要 PyTorch

# --- OCR 文字检测（可选：需要下载模型） ---
# paddleocr  # 需要 PaddlePaddle 环境

# --- 工具 ---
python-dotenv>=1.0.0
aiofiles>=23.0.0
